# 🚀 未來功能企劃與目標清單 (Future Goals)

本文檔記錄 GridSight 未來預計加入的四大核心功能擴充計畫，包含技術可行性與預期實作方向。以下功能目前皆列為 **未來目標 (Future Goals)**。

---

## 1. 📡 學生示範廣播 (Student Showcase Relay)
- **目標**：允許教師選定某位學生的畫面，並將其畫面作為「示範畫面」即時推流轉播給全班所有學生觀看。
- **實作考量**：
  - **抑制無限遞迴 (Anti-Mirroring)**：當該名示範學生自己也在接收全班廣播時，系統必須在其本機端抑制「自己觀看自己」的全螢幕廣播彈出，否則將產生無限鏡像迴圈。此部分已在部分實作中規劃。
  - **轉播管線架構 (採用 Server Relay)**：
    - 示範學生透過反向 WebSocket 將 H.264 串流推給教師 Node.js 伺服器，再由教師伺服器重新封裝為 RTP 多播 (Multicast) 發送給全班。此方案延遲較低，並可由教師機集中管控流量，避免複雜的 P2P 穿透或防火牆問題。

## 2. 🖱️ 廣播視窗滑鼠自動跟隨 (Auto-Tracking Mouse on Broadcast)
- **目標**：解決學生端螢幕比例與解析度小於教師端時的觀看體驗問題，確保學生能看清教師操作細節，同時避免視窗劇烈晃動造成暈眩。
- **實作考量與解決方案**：
  - **雙模式渲染策略**：
    1. **全螢幕模式 (Letterbox/Pillarbox)**：採用維持原比例的縮放模式 (Aspect Ratio Fit)，確保完整顯示整個教師畫面，未填滿區域以純黑背景補齊，不進行畫面平移。
    2. **視窗模式 (1:1 Native Resolution)**：為了不降低學生觀看內容的解析度，視窗內以 1:1 原生解析度裁切顯示教師畫面。當教師畫面大於學生視窗時，啟動滑鼠跟隨平移機制。
  - **滑鼠座標與平移演算法 (視窗模式專屬)**：
    - **座標中繼**：透過現存的 RTP 或 WebSocket 協定，將教師端滑鼠的絕對座標 (`X, Y`) 即時派發給學生端。
    - **邊界死區觸發 (Deadzone)**：定義畫面中央約 60%~70% 的區域為「死區」。只有當教師滑鼠離開死區，靠近畫面邊緣時，學生端的 viewport 才會開始向該方向平移，以專注教師滑鼠附近的內容。
    - **平滑限速與防暈眩 (Velocity Clamping & Damping)**：
      - 禁止 viewport 座標 1:1 絕對綁定教師滑鼠。
      - 實作基於低通濾波 (Low-pass Filter) 或彈簧物理模型 (Spring Physics) 的平滑插值演算法。
      - 設定最高平移速率上限 (Max Velocity)。即使教師滑鼠瞬間快速飄移到螢幕另一端，學生端的視窗畫面也會以恆定、柔和的最大速度滑動過去，絕不產生瞬間跳幀或快速劇烈滑動。

## 3. 🖥️ 廣播視窗全螢幕模式 (Immersive Fullscreen Broadcast)
- **目標**：強化目前的廣播視窗體驗，允許教師端透過協定發送指令，強制學生端進入全螢幕模式。
- **實作考量**：
  - **通知協定擴充**：目前的 RTP 廣播接收已具備 `[F]` 鍵或雙擊切換全螢幕的功能（預設為視窗模式）。需在廣播啟動通知的 WebSocket Payload 或 RTP 控制封包中新增 `fullscreen: true` 的命令標記。
  - **自動切換**：學生端 `rtp_receiver.cpp` 解析到該指令後，自動呼叫現存的全螢幕切換邏輯，無須學生手動操作。

## 4. 🆔 學號登錄 (Student ID Login System)
- **目標**：適應不同班級在相同實體電腦教室上課的情境，允許學生快速登錄學號以對應目前座位，無需複雜的事前審查或名單匯入機制。
- **實作考量**：
  - **Agent 彈窗提示**：學生端 `gs-agent` 啟動或收到指令時，跳出一個輕量的 Win32 彈窗，提示學生輸入學號。
  - **Console 狀態更新**：學生輸入學號後，Agent 將資料回傳，教師端 (GridSight Console) 直接更新該座位卡片顯示的學號/名稱。
  - **零審查機制**：採用信任制，後端無需查驗該學號是否在名單內，隨填即用，降低不同班級切換時的維運成本。