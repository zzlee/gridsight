{
  "name": "gridsight",
  "version": "5.8.6",
  "private": true,
  "type": "module",
  "scripts": {
    "build:windows": "node scripts/build-windows-console.js",
    "build:portable": "node scripts/build-windows-portable.js",
    "build": "npm --prefix console run build",
    "dev": "npm --prefix console run dev",
    "server": "npm --prefix console/server run start",
    "build:docker": "./scripts/build-docker.sh"
  }
}
